pipeline {
    agent none
    stages {
        stage('lint') {
            agent any;
            steps {
                bat '''
                    docker build -t pemjh:lint --target test .
                    docker run --rm -t -v ${env.WORKSPACE}:/usr/src/app pemjh:lint
                '''
            }
        }
    }
}