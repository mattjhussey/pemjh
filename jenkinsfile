pipeline {
    agent none
    stages {
        stage('lint') {
            agent {
                dockerfile {
                    filename 'Dockerfile'
                    additionalBuildArgs '--target lint'
                    args "-t -v ${env.WORKSPACE}:/usr/src/app"
                }
            }
        }
    }
}